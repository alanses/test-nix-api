###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################
version: "3.1"
services:

    mysql:
      image: mysql:5.7
      container_name: api-nix-test-mysql
      working_dir: /application
      volumes:
        - .:/application
      environment:
        - MYSQL_ROOT_PASSWORD=api-nix-test
        - MYSQL_DATABASE=api-nix-test
        - MYSQL_USER=api-nix-test
        - MYSQL_PASSWORD=api-nix-test
      ports:
        - "8084:3306"

    webserver:
      image: nginx:alpine
      container_name: api-nix-test
      working_dir: /application
      volumes:
          - .:/application
          - ./phpdocker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      ports:
       - "9991:80"

    php-fpm:
      build: phpdocker/php-fpm
      container_name: api-nix-test-php-fpm
      working_dir: /application
      volumes:
        - .:/application
        - ./phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/7.3/fpm/conf.d/99-overrides.ini

    phpmyadmin:
      image: phpmyadmin/phpmyadmin
      links:
        - mysql:mysql
      ports:
        - 9999:80
      environment:
        MYSQL_USERNAME: api-nix-test-php-fpm
        MYSQL_ROOT_PASSWORD: api-nix-test-php-fpm
        PMA_HOST: mysql
